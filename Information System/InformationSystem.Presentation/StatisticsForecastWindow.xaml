<Window x:Class="InformationSystem.Presentation.StatisticsForecastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Прогноз выручки по кварталам"
        Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Прогноз выручки по кварталам"
                   FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <ComboBox x:Name="ChartTypeComboBox"
                      Width="200"
                      SelectedIndex="0"
                      Style="{StaticResource MaterialDesignDataGridComboBox}">
                <ComboBoxItem Content="Линейная"/>
                <ComboBoxItem Content="Столбчатая"/>
            </ComboBox>
            <Button Content="Построить график"
                    Margin="10,0,0,0"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="BuildChart_Click"/>
        </StackPanel>

        <lvc:CartesianChart x:Name="RevenueChart" Grid.Row="2"/>
    </Grid>
</Window>
